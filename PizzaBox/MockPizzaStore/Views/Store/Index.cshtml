
@{
    ViewData["Title"] = "Choose a Location";
}

<h1>Choose a Location</h1>

<h1>@TempData.Peek("Cart")</h1>

@{
    
        foreach (var store in ViewBag.stores)
        {
            <p>@store.StoreAddress</p>
        }

    TempData.Keep("Cart");
}